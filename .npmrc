# Only install package versions that are at least 7 days old
min-release-age=7

# Do not run lifecycle scripts from installed packages
ignore-scripts=true

# Recommended for reproducible CI installs
package-lock=true
audit=true
fund=false
